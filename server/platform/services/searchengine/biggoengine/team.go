package biggoengine

import (
	"git.biggo.com/Funmula/mattermost-funmula/server/public/model"
)

func (be *BiggoEngine) SearchTeams(userId string, searchParams []*model.SearchParams, page, perPage int) (result []string, aErr *model.AppError) {
	return
}
